import React from 'react';import {Pressable,Text,View} from 'react-native';import {AppShell,FeatureGate,Card,Pill,SectionHeader} from '../components/ui';import {C} from '../lib/theme';
export default function Pipeline(){return <FeatureGate featureKey="pipeline"><AppShell active="account" title="Data pipeline" subtitle="Weekly refresh and source health"><Card><SectionHeader title="Weekly extraction" subtitle="The production pipeline is controlled by the Windows admin console and publishes validated reports to Cloudflare D1."/><View style={{gap:10,marginTop:14}}>{[['01','Fetch','Pull source datasets for every enabled country'],['02','Validate','Check freshness, schema, missing values and source health'],['03','Score','Run country-specific opportunity models'],['04','Publish','Write dated report snapshots to D1'],['05','Surface','Mobile and web clients read the latest country snapshot']].map(x=><View key={x[0]} style={{flexDirection:'row',gap:12,backgroundColor:C.panel2,padding:13,borderRadius:12}}><Text style={{color:C.accent,fontWeight:'900'}}>{x[0]}</Text><View style={{flex:1}}><Text style={{color:C.text,fontWeight:'800'}}>{x[1]}</Text><Text style={{color:C.muted,fontSize:11,marginTop:3}}>{x[2]}</Text></View><Pill tone="good">READY</Pill></View>)}</View></Card><Card><SectionHeader title="Source governance"/><Text style={{color:C.muted,fontSize:12,lineHeight:19,marginTop:10}}>Every country has an explicit source registry. The platform keeps report dates and retrieval timestamps so an investor can see when a signal was last refreshed and where it came from.</Text></Card></AppShell></FeatureGate>}
