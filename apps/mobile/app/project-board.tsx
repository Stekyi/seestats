import React from 'react';import {Text,View} from 'react-native';import {AppShell,FeatureGate,Card,Pill,SectionHeader} from '../components/ui';import {C} from '../lib/theme';
const cols=[['NEXT',['Confirm source freshness','Request 3 supplier quotes']],['IN PROGRESS',['Build landed-cost model','Identify buyer shortlist']],['DONE',['Rank weekly opportunities','Review margin threshold']]];
export default function ProjectBoard(){return <FeatureGate featureKey="project_board"><AppShell active="account" title="Project board" subtitle="Keep the investment thesis moving"><View style={{flexDirection:'row',gap:12,flexWrap:'wrap'}}>{cols.map(c=><Card key={c[0]} style={{flex:1,minWidth:250}}><SectionHeader title={c[0]}/><View style={{gap:8,marginTop:10}}>{c[1].map(t=><View key={t} style={{backgroundColor:C.panel2,borderRadius:11,padding:12}}><Text style={{color:C.text,fontSize:12}}>{t}</Text><Text style={{color:C.muted,fontSize:10,marginTop:4}}>See Stats action</Text></View>)}</View></Card>)}</View><Card><Text style={{color:C.muted,fontSize:11,lineHeight:17}}>The board is intentionally lightweight. It exists to connect the statistical signal to diligence work, not to replace a full project-management platform.</Text></Card></AppShell></FeatureGate>}
